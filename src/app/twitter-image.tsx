// Twitter card reuses the Open Graph image. Next.js will look for
// this file and emit a `<meta name="twitter:image">` tag pointing at
// the generated asset.
export { default, alt, size, contentType } from './opengraph-image';
